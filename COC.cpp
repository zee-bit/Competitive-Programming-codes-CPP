#include <iostream>
#include <vector>
using namespace std;
typedef long ll;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	ll n;
	cin>>n;
	vector<int> a(n);
	vector<int> b(n);
	vector<int> c(n);
	for(ll i=n-1; i>=0; i--) {
		if(b[i]-a[i]>0)
	}
	return 0;
}